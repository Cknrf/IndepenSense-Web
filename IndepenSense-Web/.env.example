# Backend origin (no trailing slash, no /web suffix — the app appends /web).
# Local dev: point at your NestJS server.
# Railway: set to the backend service's public URL, e.g. https://backend-production-xxxx.up.railway.app
VITE_API_URL=http://localhost:3000
